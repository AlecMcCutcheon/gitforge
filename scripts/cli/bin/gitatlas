#!/usr/bin/env bash
# Deprecated: use `gitforge`.
set -euo pipefail
echo "gitatlas is deprecated; use gitforge" >&2
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"
exec npx --yes tsx "${ROOT}/scripts/cli/gitforge.ts" "$@"
